# Atana Knowledge Graph Engine

Operational graph inside Atana-IM-Tasks.html (Graph tab), version 3.6.1.
Production target remains Neo4j + GraphQL + .NET 9 + Azure OpenAI.

## Ontology

Client requirement → OIR → PIR → EIR → AIR → Information Template → Information Requirement → Scope Production Package → Deliverable → IDS → AIM → Asset → Digital Twin → Operations.

Node kinds and relationship kinds are listed live under Graph → Schema.

FB01 seed reverse walk: Fire Rating → Door template → AIR → EIR → PIR → OIR.

## In the app

Graph tabs: Trace, Impact, Ask, Queries, Dashboards, Schema.
Decide Copilot uses the same answers.
